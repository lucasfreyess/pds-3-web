<%= form_with(model: controladorr) do |form| %>
  <% if controladorr.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(controladorr.errors.count, "error") %> prohibited this controladorr from being saved:</h2>

      <ul>
        <% controladorr.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :esp32_mac_address, style: "display: block" %>
    <%= form.text_field :esp32_mac_address %>
  </div>

  <div>
    <%= form.label :locker_count, style: "display: block" %>
    <%= form.number_field :locker_count %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
